[workspace]
resolver = "3"
members = [
    "crates/core",
    "crates/api",
    "crates/ui",
    "crates/cashcode",
    "crates/printer",
    "crates/database",
]

[profile.release]
opt-level = 3
debug = false
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false
